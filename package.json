{
  "name": "the-owl-atlas",
  "version": "0.3.0",
  "private": true,
  "description": "An illustrated, source-linked editorial atlas of Athenian owl coinage.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "build:local": "node build.mjs --local-images",
    "vendor:images": "node scripts/vendor-images.mjs",
    "check": "node scripts/check.mjs",
    "build:deploy": "node build.mjs && node scripts/prepare-deploy.mjs",
    "build:deploy:local": "node build.mjs --local-images && node scripts/prepare-deploy.mjs"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
